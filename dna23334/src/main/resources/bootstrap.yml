server:
  port: 23334

spring:
  application:
    name: dna
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        prefix: commonWithDB
  profiles:
    active: dev

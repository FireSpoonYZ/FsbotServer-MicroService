server:
  port: 23337

spring:
  application:
    name: dice
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        prefix: common
  profiles:
    active: dev

server:
  port: 23330

spring:
  application:
    name: fate
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        prefix: commonWithDB
  profiles:
    active: dev
